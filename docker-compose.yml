version: '3.9'
services:
  voicevox:
    image: hiroshiba/voicevox_engine:nvidia-latest@sha256:aca3621e814b3819534be8cdddac24eb235ee64e0c03bffdbc8c609be659ee7f
    hostname: voicevox
    container_name: discordtts__voicevox
    restart: always
    expose:
      - 50021

    networks:
      - voicevox_discordtts

    deploy:
      resources:
        limits:
          memory: 8g
        reservations:
          devices:
            - driver: nvidia
              capabilities: [gpu]

    logging:
      driver: json-file
      options:
        max-file: '1'
        max-size: 1m

  discordtts:
    build: .
    image: discordtts
    hostname: discordtts
    container_name: discordtts__discordtts
    depends_on:
      - voicevox

    restart: always

    environment:
      VOICEVOX_HOST: http://voicevox:50021
      STATE_PATH: /var/discordtts/state.json
      TMP_PATH: /tmp/
      DISCORD_TOKEN: ${DISCORD_TOKEN}

    volumes:
      - type: volume
        source: discordtts__data
        target: /var/discordtts/

    networks:
      - voicevox_discordtts
      - discordtts_internet

    deploy:
      resources:
        limits:
          memory: 512m

    logging:
      driver: json-file
      options:
        max-file: '1'
        max-size: 1m

volumes:
  data:
    name: discordtts__data

networks:
  voicevox_discordtts:
    name: discordtts__voicevox_discordtts
    internal: true

  discordtts_internet:
    name: discordtts__discordtts_internet
